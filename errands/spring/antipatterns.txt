Обнаруженные антипаттерны на текущем разрабатываемом проекте
1) Blind faith // Слепая вера
    Приходящие RequestBody объекта с фронта на контроллеры не проверяются.
    Добавить проверку. BindingResult
2)Improbability factor // Фактор невероятности
    Сервисный слой, получение данных из Optional методом get().
    Заменить на orElseThrow() с обработкой ошибки
3) Спагетти-код // Полтергейст
    Бесполезный кусок с Map cache в сервисном слое. (реализация шаблон Identity Map)
    Вырезать полностью.

    Разноплановая логика обработки POST, PUT, DELETE запросов. Где-то void, где-то boolean, где-то ResponseEntity
    Привести к однообразному состоянию. Возвращать ResponseEntity  с измененным/созданным body и HTTP.статусом о успешном или не успешном выполнении.

